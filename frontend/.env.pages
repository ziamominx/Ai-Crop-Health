# Loaded only by `npm run build:pages` (vite --mode pages).
# The published build has no Python backend, so the app answers /api/* from the
# bundled dataset in src/demo/fixtures.json instead of calling the API.
VITE_STATIC_DEMO=true
VITE_API_BASE=
